meta {
  name: Create question
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/questions
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "question": "How many days are there in a week?",
    "answer": "Seven"
  }
}
